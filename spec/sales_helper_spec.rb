describe SalesHelper do
  describe "#active_sale" do
    it "returns a boolean" do
      # sale = Sale.new(name: 'March Break', start_date_)
      # expect(Sale.active.any?).to eql(7)
    end
  end
end